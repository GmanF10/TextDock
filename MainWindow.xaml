<Window x:Class="TextDock.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:models="clr-namespace:TextDock.Models"
        xmlns:controls="clr-namespace:TextDock.Controls"
        mc:Ignorable="d"
        Title="TextDock"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Width="600"
        Height="400"
        WindowStartupLocation="CenterScreen">
    <Border x:Name="WindowBorder"
            Background="#F0F8FF"
            BorderBrush="#B0C4DE"
            BorderThickness="1"
            CornerRadius="12"
            MouseLeftButtonDown="Border_MouseLeftButtonDown">
        <Canvas x:Name="IconCanvas"
                Margin="16">
            <ItemsControl ItemsSource="{Binding FileItems}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Canvas.Left" Value="{Binding X}" />
                        <Setter Property="Canvas.Top" Value="{Binding Y}" />
                    </Style>
                </ItemsControl.ItemContainerStyle>
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type models:FileItem}">
                        <controls:FileIconControl FileName="{Binding FileName}"
                                                  IsSelected="{Binding IsSelected}"
                                                  MouseLeftButtonDown="FileIconControl_MouseLeftButtonDown"
                                                  MouseDoubleClick="FileIconControl_MouseDoubleClick" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Canvas>
    </Border>
</Window>
